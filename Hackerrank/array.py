import numpy as np

# arr = np.array(input().split())
# arr = np.flip(np.array(arr, float))
print(np.flip(np.array(input().split(), float)))
#ehmm, maybe it's look cool, but it is cool, all in one line @@ but hopefully it's still clear
#for me (after a period of time) or someone else to read it